// /india-consult replicates /consult exactly — a second URL for the same
// page so the India layoff-anxiety Meta campaign has its own path to track,
// not a second page to maintain. See src/app/us-consulting/page.tsx for the
// identical pattern used by the US campaign.
export { metadata } from '../consult/page'
export { default } from '../consult/page'
